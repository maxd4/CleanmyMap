name: repo-governance

on:
  push:
  pull_request:

jobs:
  top-heavy-files:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Node
        uses: actions/setup-node@v6
        with:
          node-version: "20"

      - name: Enforce top heavy files policy
        run: npm run quality:top-heavy
