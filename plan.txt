PLAN D EXECUTION CONVERSATIONNEL - CLEANMYMAP
Date: 2026-04-09
Version: 2.0 (synthese actionnable)

OBJECTIF
- Continuer l amelioration du site avec des messages courts, ordonnes et reutilisables.
- Eviter le bruit historique; garder uniquement: etat atteint, dette residuelle, prochaine sequence.

================================================================
ETAT ACTUEL (RESUME FIABLE)
================================================================
- Phases 1 a 7 executees.
- Auth Clerk/Google live operationnelle.
- Navigation/sections unifiees via registre unique.
- Map/form/export alignes sur un contrat commun.
- Moderation admin branchee UI -> API.
- Scripts de verification rapide ajoutes:
  - scripts/check_changed_quick.ps1
  - scripts/run_focus.ps1
- Build/lint passent localement.

Points encore ouverts (non bloquants immediats):
- Warning Sentry release/sourcemaps si SENTRY_AUTH_TOKEN absent.
- Integrations optionnelles non connectees (sentry, posthog, resend, etc.).
- Quelques textes FR encore perfectibles dans zones heritagees.

================================================================
DETTE RESIDUELLE PRIORISEE
================================================================
CRITIQUE
- Aucun blocage critique confirme cote code/build/lint.

IMPORTANT
1. Finaliser signal monitoring prod (uptime critique vs optionnel) et verifier affichage panneau systeme.
2. Normaliser les derniers textes FR visibles (coherence nomenclature et accents) sur pages prioritaires.
3. Stabiliser la doc exploitation (auth live, checks minimaux, parcours smoke).

UTILE
1. Brancher SENTRY_AUTH_TOKEN pour release + sourcemaps.
2. Finaliser branchement PostHog/Resend selon besoin produit.
3. Ajouter note runbook "migration Clerk live" (roles/admin metadata).

================================================================
MESSAGES PRETS A RENVOYER (PROCHAINE MINI-SEQUENCE)
================================================================
Message A
Type: hardening prod cible
But: fiabiliser le monitoring runtime
Message exact:
"Verifie et finalise /api/uptime + panneau Etat des integrations: status critique lisible, optionnels en warning non bloquant, et coherence UI/API. Fais les patches minimaux puis valide lint/build."

Message B
Type: qualite UX texte
But: finir harmonisation FR prioritaire
Message exact:
"Passe sur les pages web prioritaires (dashboard, declaration, map, reports, admin) et corrige uniquement les textes FR encore degrad s/incoherents, sans changer le scope metier ni le design."

Message C
Type: validation non regression courte
But: verrouiller un protocole stable
Message exact:
"Execute la matrice courte: npm --prefix apps/web run lint, npm --prefix apps/web run build, npm run checks:changed:quick. Donne un rapport court par check avec seulement ecarts reels."

Message D
Type: docs ops minimale
But: clore proprement la vague technique
Message exact:
"Mets a jour la doc ops minimale (architecture/checks/auth live) pour refleter l etat actuel, sans ouvrir de nouveau chantier."

Message E
Type: pre-phase suivante
But: preparer la prochaine vague produit
Message exact:
"Fais un bilan final compact: changements effectifs, impacts, limites restantes, dette priorisee (critique/important/utile), et propose 3 options de prochaine phase avec effort estime (S/M/L)."

================================================================
REGLES D EXECUTION
================================================================
- Toujours patcher de maniere prudente (pas de suppression agressive non prouvee).
- Garder compatibilite des routes actives.
- Ne pas masquer les erreurs reelles dans les scripts/logs.
- Valider systematiquement lint/build apres modifications structurelles.

Ce document est le guide operatoire courant.
