# Image and Cache Patterns

Prefer these patterns when the change touches media or caching:

- lazy load heavy components when appropriate
- avoid shipping extra assets to every route
- keep cache behavior explicit
- re-check the route after changing image or data-loading strategy
