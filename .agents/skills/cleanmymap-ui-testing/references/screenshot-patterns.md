# Screenshot Patterns

Prefer deterministic checks:

- stable selectors over text-only guesses
- a screenshot when the visual state matters
- one happy path and one edge case
- no flaky waits unless the app genuinely needs them

Capture the smallest useful surface instead of the whole browser session.
