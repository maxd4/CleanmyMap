"use client";

/**
 * Ancien écran de choix du mode d'affichage.
 * La sélection a été déplacée dans le flux de création de compte.
 * Le composant est conservé comme point d'extension, mais ne rend plus rien.
 */
export function DisplayModeOnboardingGate() {
  return null;
}
