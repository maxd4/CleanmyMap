/**
 * Compatibility façade for the unified action source.
 *
 * The implementation is split by responsibility under `unified-source/` so
 * existing imports do not need to change.
 */
export * from "./unified-source/index";
